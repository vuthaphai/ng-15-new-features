import { ItalicsFontDirective } from './italics-font.directive';

describe('ItalicsFontDirective', () => {
  it('should create an instance', () => {
    const directive = new ItalicsFontDirective();
    expect(directive).toBeTruthy();
  });
});
