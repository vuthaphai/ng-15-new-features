export function add(a:number ,b:number): number{
    return a+b
}
export function substract(a:number ,b:number): number{
    return a-b
}

export const calculateArea = (height: number, width: number) => height * width;

export const calculatePerimeter = (height: number, width: number) => 2 * ( height * width);
